type Query {
    users: [User]
}

type User {
    id: ID!
    password_hash: String,
    first_name: String,
    last_name: String,
    email: String,
    phone_number: String,
    birth_date: Float,
    loan_rating: Float,
    borrow_rating: Float
}